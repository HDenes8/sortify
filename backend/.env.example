# copy this file to .env and fill in values for production or local dev
SECRET_KEY=replace-with-a-secret
# the DATABASE_URL should be provided by your hosting provider (Render, Railway, etc.)
DATABASE_URL=postgresql://postgres:password@localhost:5432/sortify
# optional CORS_ORIGINS can be a comma separated list or *
CORS_ORIGINS=
